/**
 * Global default configuration for svelte-multistyle-ui.
 *
 * Import `initMultistyleUI` once in your app (e.g., in `+layout.svelte` or
 * `App.svelte`) to set the default `style` and `theme` for every component.
 * Per-component `style`/`theme` props still override these defaults.
 */

export { defaults, initMultistyleUI, iconClass } from "./config.svelte.js";
